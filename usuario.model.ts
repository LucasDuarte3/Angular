export class Usuario {
    id!: number; //o operador ! fala para o java scriptque a variável será gravada em cache, não tem como subscrever.
    nome!: string;
    sobrenome!: string;
    username!: string;
    password!: string;
}
